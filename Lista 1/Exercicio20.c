#include <stdio.h>

int main(){

    float kg, libras;

    printf("Digite o valor Kg: ");
    scanf("%f", &kg);

    libras = kg / 0.45;

    printf("O resultado: %f", libras);

return 0;
}
