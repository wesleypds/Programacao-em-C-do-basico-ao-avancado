#include <stdio.h>
#include <stdlib.h>

int main(){

    int num, maior = 0, menor = 0, num1, num2, i = 0, qtd = 0;

    srand(time(NULL));

    do{
        printf("Digite um numero: ");
        scanf("%d", &num);
        num1 = num;
        num2 = num;
        if(){

        }
    }while(i < 10);

    printf("O menor numero: %d\n", menor);
    printf("O maior numero: %d\n", maior);

    return 0;
}
